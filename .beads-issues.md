# BUG-001: Inventory.list_items() Returns Wrong Type

**Severity:** P1
**Status:** Open

list_items() returns a list instead of dict[str, Item]. Causes AttributeError when code tries to access dict methods.

**Location:** rag_quest/engine/inventory.py

**Reproduction:**
```python
inv = Inventory()
inv.add_item("Sword", "A blade", quantity=1, weight=5)
items = inv.list_items()
# Returns: ['common_item', 'uncommon_item', ...] instead of dict
```

**Expected Behavior:** Return dict[str, Item] mapping names to items

---

# BUG-002: Party Initialization Broken

**Severity:** P1
**Status:** Open

Party.__init__() doesn't accept 'leader' keyword argument

**Location:** rag_quest/engine/party.py

**Error:** TypeError: __init__() got an unexpected keyword argument 'leader'

**Expected:** `Party(leader=character)` should work

---

# BUG-003: RelationshipManager Missing add_npc()

**Severity:** P1
**Status:** Open

add_npc() method doesn't exist, cannot add NPCs to track.

**Location:** rag_quest/engine/relationships.py

**Expected:** RelationshipManager should have add_npc(name, role) method

---

# BUG-004: QuestLog.add_quest() API Mismatch

**Severity:** P2
**Status:** Open

add_quest() signature doesn't match usage. Called with Quest object but expects different args.

**Location:** rag_quest/engine/quests.py

**Error:** TypeError: missing 1 required positional argument

---

# BUG-005: EventType Enum Incomplete

**Severity:** P2
**Status:** Open

EventType is missing common event types like CONFLICT, DISCOVERY, etc.

**Location:** rag_quest/engine/events.py

**Expected:** Define all standard EventType enum values

---

# BUG-006: Character Missing Ability Methods

**Severity:** P2
**Status:** Open

Character has CLASS_ABILITIES defined but no method to get available abilities at current level.

**Location:** rag_quest/engine/character.py

**Missing:** get_available_abilities() method

---

# IMPROVEMENT-001: Inventory Iteration

**Severity:** P3
**Status:** Open

Inventory item listing is broken. Need consistent way to iterate over items.

---

# IMPROVEMENT-002: API Consistency

**Severity:** P3
**Status:** Open

Inconsistent naming patterns across modules (list_items vs get_active_quests, etc.)

---

# BUG-007: WebUI Turn Counter Does Not Increment

**Severity:** P2
**Status:** Open

After submitting a turn via the WebUI, the header still shows `(turn 0)`. The `done` SSE event
includes the updated `state` with the incremented `turn_number`, but `handleSseEvent()` never
calls `setStatus()` to refresh the header.

**Location:** rag_quest/web/static/index.html — `handleSseEvent()` done branch (~line 478)

**Expected Behavior:** Header should update to `Kael — Test World (turn 1)` after first turn.

---

# BUG-008: WebUI Narrative Renders Markdown Asterisks Literally

**Severity:** P2
**Status:** Open

LLM narrator output contains markdown bold markers (`**Aethelgard**`). The web client uses
`textContent` (correct for XSS safety) which renders them as literal asterisks instead of bold
text. Needs a safe markdown-to-DOM conversion that uses `createElement`/`textContent` only.

**Location:** rag_quest/web/static/index.html — chunk event handler (~line 470)

**Expected Behavior:** `**bold**` should render as bold text; `*italic*` as italic.

---

# BUG-009: WebUI Inventory Sidebar Shows (empty) Despite Item Pickup

**Severity:** P3
**Status:** Open

After the narrator describes the player picking up an item, the sidebar inventory still reads
`(empty)`. The `renderState()` call expects `inventory.items` to be a list, but `Inventory.to_dict()`
serializes items as a dict keyed by name. The JS check `Array.isArray(inv.items)` fails on
the dict, so it falls through to the empty display.

**Location:** rag_quest/web/static/index.html — `renderState()` inventory section (~line 366)

**Expected Behavior:** Inventory sidebar should display any items present in the serialized state.

---
